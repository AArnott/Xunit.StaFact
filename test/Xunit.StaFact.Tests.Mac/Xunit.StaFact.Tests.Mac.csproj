<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net6.0-macos</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>true</ImplicitUsings>
    <SupportedOSPlatformVersion>10.15</SupportedOSPlatformVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <HasUnitTests>true</HasUnitTests>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="xunit.runner.console" />
    <PackageReference Include="xunit" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Xunit.StaFact\Xunit.StaFact.csproj" />
  </ItemGroup>
  <ItemGroup>
    <Reference Include="$(XunitConsoleNetCore2AppPath)" />
  </ItemGroup>

  <Target Name="Test" AfterTargets="Build">
    <PropertyGroup>
      <_PartalAppExePath>$(MSBuildThisFileDirectory)..\..\bin\Xunit.StaFact.Tests.Mac\$(Configuration)\$(TargetFramework)\osx-x64</_PartalAppExePath>
      <_AppExePath>$(_PartalAppExePath)\Xunit.StaFact.Tests.Mac.app\Contents\MacOS\Xunit.StaFact.Tests.Mac</_AppExePath>
    </PropertyGroup>
    <!-- TODO: Verify these paths and use these arguments in AppDelegate.cs -->
    <Exec Command="&quot;$(_AppExePath)&quot; xunit &quot;$(_PartalAppExePath)&quot; -xml &quot;$(ArtifactStagingFolder)test_logs\$(TargetName).xml&quot;" />
  </Target>

  <Target Name="Message" DependsOnTargets="Build">
    <Message Text="Build test MSBuildThisFileDirectory is $(MSBuildThisFileDirectory)" Importance="High" />
  </Target>

  <!-- Prevent extraneous warnings, see https://github.com/xamarin/xamarin-macios/issues/15897 -->
  <Target Name="_SetPublishFolderTypeNoneOnDocFileItems" BeforeTargets="_ComputePublishLocation">
    <ItemGroup>
      <ResolvedFileToPublish
        Update="@(ResolvedFileToPublish)"
        Condition="'%(ResolvedFileToPublish.Extension)' == '.xml' And '%(ResolvedFileToPublish.PublishFolderType)' == ''"
        PublishFolderType="None" />
    </ItemGroup>
  </Target>
</Project>
